export default function AccountSettingsPage() {
  return (
    <section className="page">
      <h1>Account settings</h1>
      <p>
        Profile, security, and linked identity settings are managed through
        backend-authoritative account endpoints.
      </p>
      <div className="notice">
        <p>This page is intentionally minimal in the initial implementation pass.</p>
      </div>
    </section>
  );
}
